// User allowlist removed — registration is now open to any username
// matching the standard validation rule.
export const allowedUsers: string[] = [];

export const isAllowedUser = (_username: string) => true;